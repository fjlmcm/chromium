diff --git a/components/embedder_support/user_agent_utils.cc b/components/embedder_support/user_agent_utils.cc
index 38e4917692..3d56d8f8f2 100644
--- a/components/embedder_support/user_agent_utils.cc
+++ b/components/embedder_support/user_agent_utils.cc
@@ -25,6 +25,7 @@
 #include "components/embedder_support/switches.h"
 #include "components/policy/core/common/policy_pref_names.h"
 #include "components/prefs/pref_service.h"
+#include "components/ungoogled/ungoogled_switches.h"
 #include "components/version_info/version_info.h"
 #include "content/public/browser/web_contents.h"
 #include "content/public/common/content_features.h"
@@ -262,9 +263,12 @@ std::string GetUserAgentInternal(
   // desktop UA strings.
   // In User-Agent reduction phase 6, only apply the <unifiedPlatform> to
   // android UA strings.
-  return ShouldSendUserAgentUnifiedPlatform(user_agent_reduction)
+  std::string user_agent = ShouldSendUserAgentUnifiedPlatform(user_agent_reduction)
              ? content::BuildUnifiedPlatformUserAgentFromProduct(product)
              : content::BuildUserAgentFromProduct(product);
+  // 添加品牌信息到 User-Agent
+  user_agent += blink::GetUserAgentFingerprintBrandInfo();
+  return user_agent;
 }
 
 // Generate random order list based on the input size and seed.
diff --git a/content/browser/client_hints/client_hints.cc b/content/browser/client_hints/client_hints.cc
index 69b7169dfc..bc0b5ed6b0 100644
--- a/content/browser/client_hints/client_hints.cc
+++ b/content/browser/client_hints/client_hints.cc
@@ -709,6 +709,8 @@ void UpdateNavigationRequestClientUaHeadersImpl(
     if (!ua_metadata.has_value())
       ua_metadata = delegate->GetUserAgentMetadata();
 
+    UpdateUserAgentMetadataFingerprint(&ua_metadata.value());
+
     // The `Sec-CH-UA` client hint is attached to all outgoing requests. This is
     // (intentionally) different than other client hints.
     // It's barred behind ShouldAddClientHints to make sure it's controlled by
diff --git a/content/common/user_agent.cc b/content/common/user_agent.cc
index 761d0650f5..1c6f85a105 100644
--- a/content/common/user_agent.cc
+++ b/content/common/user_agent.cc
@@ -10,7 +10,11 @@
 #include "content/public/common/user_agent.h"
 
 #include <stdint.h>
+#include <algorithm>
+#include <cctype>
+#include <string>
 
+#include "base/command_line.h"
 #include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/strcat.h"
@@ -19,6 +23,8 @@
 #include "base/system/sys_info.h"
 #include "build/build_config.h"
 #include "build/util/chromium_git_revision.h"
+#include "components/ungoogled/ungoogled_switches.h"
+
 
 #if BUILDFLAG(IS_MAC)
 #include "base/mac/mac_util.h"
@@ -46,6 +52,21 @@ const char kFrozenUserAgentTemplate[] =
     "Safari/537.36";
 
 std::string GetUserAgentPlatform() {
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  if (command_line->HasSwitch(switches::kFingerprintPlatform)) {
+    std::string platform_value = base::ToLowerASCII(
+        command_line->GetSwitchValueASCII(switches::kFingerprintPlatform));
+
+    if (platform_value == "windows") {
+      return "";
+    } else if (platform_value == "linux") {
+      return "X11; ";
+    } else if (platform_value == "macos") {
+      return "Macintosh; ";
+    }
+  }
+
+
 #if BUILDFLAG(IS_WIN)
   return "";
 #elif BUILDFLAG(IS_MAC)
@@ -66,6 +87,20 @@ std::string GetUserAgentPlatform() {
 }
 
 std::string GetUnifiedPlatform() {
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  if (command_line->HasSwitch(switches::kFingerprintPlatform)) {
+    std::string platform_value = base::ToLowerASCII(
+        command_line->GetSwitchValueASCII(switches::kFingerprintPlatform));
+
+    if (platform_value == "windows") {
+      return "Windows NT 10.0; Win64; x64";
+    } else if (platform_value == "linux") {
+      return "X11; Linux x86_64";
+    } else if (platform_value == "macos") {
+      return "Macintosh; Intel Mac OS X 10_15_7";
+    }
+  }
+
 #if BUILDFLAG(IS_WIN)
   return "Windows NT 10.0; Win64; x64";
 #elif BUILDFLAG(IS_MAC)
@@ -108,6 +143,19 @@ std::string GetUnifiedPlatform() {
 }

 std::string BuildOSCpuInfo(bool include_android_build_number) {
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  if (command_line->HasSwitch(switches::kFingerprintPlatformVersion)) {
+    return command_line->GetSwitchValueASCII(switches::kFingerprintPlatformVersion);
+  }
+
+  if (command_line->HasSwitch(switches::kFingerprintPlatform)) {
+    std::string platform_value = base::ToLowerASCII(
+        command_line->GetSwitchValueASCII(switches::kFingerprintPlatform));
+    if (platform_value == "windows") {
+      return "Windows NT 10.0; Win64; x64";
+    }
+  }
+
   std::string os_cpu;

 #if BUILDFLAG(IS_WIN)
@@ -200,6 +248,24 @@ std::string GetProductAndVersion(ForceMajorVersionToMinorPosition force_major_v
                                  const std::string& version_str,
                                  const blink::UserAgentBrandVersion& product_brand_version,
                                  bool enable_updated_grease_by_policy) {
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  
+  std::string brand_name = product_brand_version.brand;
+  std::string brand_version = product_brand_version.version;
+  
+  if (command_line->HasSwitch(switches::kFingerprintBrand)) {
+    brand_name = command_line->GetSwitchValueASCII(switches::kFingerprintBrand);
+  }
+  
+  if (command_line->HasSwitch(switches::kFingerprintBrandVersion)) {
+    brand_version = command_line->GetSwitchValueASCII(switches::kFingerprintBrandVersion);
+  }
+  
+  blink::UserAgentBrandVersion custom_brand_version;
+  custom_brand_version.brand = brand_name;
+  custom_brand_version.version = brand_version;
+  const blink::UserAgentBrandVersion& final_brand_version = 
+      (command_line->HasSwitch(switches::kFingerprintBrand) || 
+       command_line->HasSwitch(switches::kFingerprintBrandVersion)) ? 
+      custom_brand_version : product_brand_version;
+
   std::string product;

   if (force_major_version_to_minor_position ==
@@ -207,7 +273,7 @@ std::string GetProductAndVersion(ForceMajorVersionToMinorPosition force_major_v
     product = "Chrome/" + version_str;
   } else {
     product = base::StrCat(
-        {product_brand_version.brand, "/", product_brand_version.version});
+        {final_brand_version.brand, "/", final_brand_version.version});
   }

   return product;
